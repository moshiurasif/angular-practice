import { NgEleDirective } from './ng-ele.directive';

describe('NgEleDirective', () => {
  it('should create an instance', () => {
    const directive = new NgEleDirective();
    expect(directive).toBeTruthy();
  });
});
